function collectAnimals(...args) {
  console.log(args,reduce(function[] {
    return 
  }))
}

collectAnimals('dog','cat','mouse','jackolope','playtypus');


function combineFruit(fruit, sweets, vegetables) {
    return {}
}

combineFruit(['apple', 'pear'],
             ['cake', 'pie'],
             ['carrit'])



const vacation = {
    location: 'Burley, Idaho',
    duration: '2 weeks'

} ;
const {location, duration } = vacation

function parseSentence () {
    return `We're going to have a good time in ${location} for ${duration}`
}

const pickles = ['sweet', 'dill']

function returnFirst(items){
    // const firstItem = items[0]

    return items[0]
    console.log(returnFirst(pickles))
}

const favoriteActivities = ["magnets", "snowboarding", "philanthropy", "janitor work', 'eating']

function returnFavorites(arr){
    /*your code here*/
    return items
    return "My top three favorite activities are, " + firstFav + ", " + s
    }
    returnFavorites(favoriteActivities)







function populatePeople(names){
    // return names.map(function(name){
    //     name = name.split(" ");
    //     console.log(name) 
    //     const firstName = name[0]
           const lastName = name[1]
    
    
    return {
    //             firstName,
    //             lastName
    //         }
    // })
}
    
split breaks name at " "



    populatePeople(["Frank Peterson", "Suzy Degual", "Liza Jones"])
    //[
    //  {firstName: "Frank", lastName: "Peterson"},
    //  {firstName: "Suzy", lastName: "Degual"},
    //  {firstName: "Liza", lastName: "Jones"},
    //]

    console.log(populatePeople(["Frank Peterson", "Suzy Degual", "Liza Jones"]))